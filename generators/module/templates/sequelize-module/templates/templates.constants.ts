export const API_TAG_<%= kebabToConstant(config.name) %> = '<%= (config.name) %>';

export const <%= kebabToConstant(config.name) %>_ALREADY_EXISTS = '<%= (config.name) %> already exists';
export const <%= kebabToConstant(config.name) %>_NOT_FOUND = '<%= (config.name) %> not found';
